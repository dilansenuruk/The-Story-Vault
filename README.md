# The-Story-Vault

### Run this command to activate the relevant environment
>> .story_vault_venv\Scripts\activate

### Run this command to install all the requirements
>> pip install -r requirements.txt

### First run the Data Ingestion Pipeline
>> python DataIngestionPipeline.py
This will run the whole pipeline of data parsing, text splitting, embedding generation and VectorDB storing on your PDFs
